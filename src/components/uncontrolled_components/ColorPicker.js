import React from 'react';

function ColorPicker() {
    return (
        <div>
            <label>Choose a color:</label>
            <input type="color" />
        </div>
    );
}

export default ColorPicker;
